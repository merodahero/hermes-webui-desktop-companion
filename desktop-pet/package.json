{
  "name": "hermes-webui-desktop-companion-pet",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "tauri dev",
    "build": "tauri build",
    "bundle:app": "tauri build -- --bundles app",
    "bundle:dmg": "tauri build -- --bundles dmg"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2.0.0"
  }
}
